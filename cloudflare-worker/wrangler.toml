name = "brulee-gmail-worker"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[triggers]
crons = ["*/15 * * * *"]

# Secrets (no van al repo — `npx wrangler secret put <NOM>`):
#   GOOGLE_CLIENT_ID
#   GOOGLE_CLIENT_SECRET
#   GOOGLE_REFRESH_TOKEN
#   OPENAI_API_KEY
#   SUPABASE_URL
#   SUPABASE_SERVICE_ROLE_KEY
